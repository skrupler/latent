#!/usr/bin/env bash


# this runs the initial setup

# install

CONF="~/latent/conf/settings.conf"

source ~/latent/conf/settings.conf

for line in $CONF;do

done

case opt in:

	$PATH:wq
	

esac

if [[ -z $TESTER ]];then
	echo "EMPTY"
fi
